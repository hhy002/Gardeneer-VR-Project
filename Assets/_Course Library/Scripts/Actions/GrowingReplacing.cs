using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GrowingReplacing : MonoBehaviour
{
    public GameObject seed;
    public GameObject plant1;
    public GameObject plant2;
    public GameObject plant3;

    // Update is called once per frame
    void Replace()
    {
        // GameObject seed = Resources.Load(plant1);
        // Instantiate(plant1);

        // yield return new WaitForSeconds(4);
    
        // GameObject plant1 = Resources.Load(plant2);
        // Instantiate(plant2);

        // yield return new WaitForSeconds(4);

        // GameObject plant2 = Resources.Load(plant3);
        // Instantiate(plant3);
    }
}
